# Changelog

이 프로젝트의 배포 및 운영 관점 변경 이력을 기록한다.

버전 표기는 빌드 시점마다 `{major}.{minor}.{yymmdd}.{hh24mi}` 형식으로 생성되므로, changelog는 개별 타임스탬프 버전보다 날짜와 변경 묶음 기준으로 관리한다.

날짜는 변경이 머지된 날 기준이며, 최신 묶음이 위에 온다.

## 2026-07-03

### 수정

- `decode`: 손상되거나 외부에서 온 QR 프레임이 불가능한 필드 값(k=0, 음수 esi, 빈 심볼,
  k·gzipLen·심볼 크기 불일치)을 갖고 있으면 **decode 실행 전체가 예외로 중단**되던 문제를
  고쳤다. 이제 해당 QR 1장만 `QR_READ_ERROR`로 분류되고 나머지는 계속 진행된다. 파일 1개
  복원 중 IO 오류(디렉터리 생성/이동 실패 등)도 그 파일만 `DECODE_ERROR`가 된다.
- `decode`: 재실행 시 이전 실행이 만든 `*-success` 디렉터리 속 PNG를 다시 읽어 재복원하고
  `-success-success` 디렉터리를 만들던 문제를 고쳤다. success 디렉터리는 수집에서 제외된다.
- `capture`: "복원 가능 N/M" 집계가 decode와 다른 기준으로 파일을 묶어(메타데이터 전체 키)
  같은 relPath에 충돌 프레임이 섞이면 배너가 과대보고할 수 있던 문제를 고쳤다. 이제 decode와
  동일하게 정규화된 relPath 기준·첫 메타데이터 우선으로 묶고, 충돌하거나 경로가 잘못된
  프레임은 `unparsedPayloads`로 집계한다.
- `capture`: 종료 시 파이프라인 소비 스레드가 이미 죽어 있으면 poison-pill `put()`에서 영구
  대기(행)하던 문제를 고쳤다.
- `encode`(flat 모드)/`reencode`: 파일명 프리픽스가 비ASCII(한글 등) 문자를 전부 `_`로
  치환해 서로 다른 파일이 같은 PNG 이름으로 덮어써지던 문제를 고쳤다. flat PNG 이름에
  relPath 해시 8자리가 붙는다: `sample_txt-1a2b3c4d_001of010.png`.
- `encode`: office 변환(xlsx→csv 등)으로 두 원본이 같은 relPath가 되면 심볼 스트림이 섞여
  한쪽이 조용히 유실되던 것을, 첫 파일만 인코딩하고 나머지는 WARN 로그와 함께 건너뛰도록 했다.
- `packager unpack`: 원본이 진짜 `.txt`인 파일(예: `readme.txt`)의 확장자를 벗겨버리던
  문제를 고쳤다. pack이 기록해 두는 `target.txt` 이름 목록을 unpack이 실제로 읽어 정확히
  복원하고, 목록이 없는 구버전 패키지는 기존 휴리스틱으로 처리한다. 목록에는 중첩 아카이브
  자체의 rename도 함께 기록된다.
- `packager pack`: `.zip` 입력일 때 출력이 입력 파일을 덮어써 원본이 사라지던 문제를
  고쳤다(`<이름>-packed.zip`으로 저장). zip→jar 변환 시 STORED 디렉터리 엔트리에서
  ZipException이 나던 문제도 고쳤다.

### 성능

- fountain 피어 디코더(`LtDecoder`/`LtPeelTracker`): 심볼 하나를 복구할 때마다 대기 심볼
  전체를 훑던 전파를 "미지 소스 → 대기 심볼" 인덱스로 바꿔, 총 비용이 심볼 차수 합에
  비례하게 됐다(손실 많은 대형 파일에서 사실상 O(k·P) → O(Σ차수)).
- `encode`: QR 비트매트릭스를 그레이 래스터에 직접 그리고(중간 RGB 이미지와 색변환 제거)
  PNG 압축 레벨을 조정해, QR 1장 생성이 약 60ms→18ms로 줄었다(픽셀 출력은 비트 동일).
  동일 조건 라운드트립 벤치마크(4파일×1MB 비압축, chunk 2000) 기준 encode 51.5s→20.3s,
  decode 18.5s→12.7s, decode 피크 힙 325→258MB.
- `decode`: 이미 복원 완료된 파일의 잉여 QR(기본 repair 0.5 기준 전체의 약 1/3)은 PNG
  읽기·QR 디코드 없이 건너뛰고 곧장 success 디렉터리로 옮긴다. 잉여 QR이 늦게 도착해도
  success로 이동해, 재실행 시 가짜 INCOMPLETE가 남지 않는다. 파일 복원 시 gzip 블록을
  두 번째 사본으로 재조립하지 않고 디코더가 가진 심볼에서 곧바로 스트리밍한다(피크 힙 절반).
- `capture`: 프레임 PNG 저장이 의도와 반대로 최대 압축(가장 느린 deflate 9)으로 동작하던
  것을 빠른 레벨로 바꿨다. 중복 제거가 페이로드 전문(프레임당 ~2KB) 대신 128비트
  다이제스트만 보관해 장시간 캡처의 힙 증가를 없앴다.
- 공통: gzip 스테이징/복원의 파일 IO에 64KB 버퍼링을 추가했고, QR 이미지 디코더가 이진화
  결과를 hint 세트 간에 재사용해 실패 프레임 재시도 비용을 줄였다.

## 2026-07-02

### 추가

- `capture`가 **파일별 복원 가능 여부를 실시간 감지**한다. 받은 QR을 fountain 프레임으로
  파싱해 decode와 같은 기준으로 파일별 심볼을 집계하고, 어떤 파일이 복원 가능해지는 순간
  `[CAPTURE][DONE]` 로그를, 관측된 파일 전부가 복원 가능해지면 "slide를 정지해도 됩니다"
  배너를 출력한다. GUI 상태 표시줄에도 `복원 가능 N/M 파일`이 표시된다. 송신측 자동 정지는
  없으며(단방향 채널), 한 번도 안 잡힌 파일은 집계되지 않는다.
- `capture-manifest.json`에 `observedFiles` / `decodableFiles` / `unparsedPayloads` 필드를 추가했다.

### 변경

- `capture`의 권장 slide 속도 로그에 **방향 힌트**를 붙였다: "지금 slide Page(ms)가 N보다
  작으면 올리고, 크면 N까지 내려도 됩니다".

## 2026-06-22

### 변경

- `slide` 재생 속도 하한을 낮춰 고속 재생을 허용한다: `Page(ms)` 최소 `100`→`20`, `Black(ms)`
  최소 `0`→`1`(스피너 증감 단위도 더 촘촘하게). 입력칸 포매터가 이 범위를 그대로 방어값으로 강제한다.
- `capture`가 권장 slide 속도(`page-display-ms`)를 종료 시뿐 아니라 **실행 중 약 10초마다**(상태
  로그 주기) 갱신된 값으로 별도 줄로 출력한다.

## 2026-06-20

### 호환성 (sender ↔ receiver 동시 교체 필요)

- QR 전송이 **LT 파운틴(fountain) 심볼** 단위로 바뀌었다. 각 QR 프레임은 하나의
  파운틴 심볼이며, `esi 0..k-1`은 원본 심볼(systematic), `esi >= k`는 복구
  심볼이다. 파일은 특정 프레임이 아니라 서로 다른 심볼을 충분히(`k` 내외) 모으면
  복원된다 — 일부 프레임 손실에 견딘다. 이전 청크 기반 페이로드와 호환되지
  않으므로 **sender와 receiver를 같은 버전으로 함께 교체**해야 한다.

### 추가

- `--repair-overhead` 옵션(CLI·GUI)으로 파운틴 복구 여유분(추가 복구 심볼 비율)을
  조절할 수 있다. 캡처 손실이 잦은 환경에서 여유를 늘리면 재시도 없이 복원
  성공률이 올라간다.
- `capture`가 권장 슬라이드 표시 시간(slide dwell)을 보고하여, `slide` 재생 속도를
  캡처 처리 속도에 맞추도록 안내한다.
- `--encode-workers`를 GUI에서도 노출했다(기존 CLI 전용).

### 성능 / 빌드

- 인코딩을 파일·청크 단위로 병렬화했다(`--encode-workers`).
- QR PNG를 RGB 대신 8비트 그레이스케일로 렌더링하여 산출물 크기를 줄였다.
- receiver 네이티브를 배포 대상 플랫폼만 번들한다(기본: Windows + arm-mac).
- 명령당 배너를 한 번만 출력하도록 실행 전략을 정리하고 빌드 배너를 추가했다.

### 수정

- packager `unpack`이 이름이 바뀐 중첩 아카이브까지 재귀적으로 풀도록 고쳤다.

## 2026-06-19

### 호환성

- gzip 바이트를 Base64 없이 QR 8비트 바이트 모드로 직접 실어 페이로드 크기를
  줄였다. 캡처 측 QR 중복 제거 문자셋도 `ISO-8859-1`로 정렬했다.

### 추가

- `--in`/`--out` 생략 시 jar 기준 기본 디렉터리(`source`/`encoded`/`captured`/
  `decoded`)를 사용하며, jar 옆 `airbridge-paths.properties`로 재정의할 수 있다.
- 캡처 장치 선택을 개선했다(GUI 드롭다운 + CLI 이름 매칭). Windows 장치 이름
  표기, 프로브 타임아웃, 폴더 선택기 동작을 함께 수정했다.
- 모든 명령 시작 시 배너를, 캡처 준비 완료 시 ASCII READY 배너를 출력한다.
- `encode-root` 검증 및 서브커맨드 설명 현지화(`--lang ko|en`).

### 성능

- 인코딩·디코딩 페이로드를 메모리에 들고 있지 않고 임시 파일로 스트리밍하며,
  디코딩 복원물을 완료 시 기록하고 evict하여 디코딩 메모리 사용을 제한한다.

### 수정

- 큰 인코딩 페이로드에서 정수 절삭/오버플로를 방지했다.
- 알려진 encode/slide 버그를 정리하고 빌드 산출물을 추적에서 제외했다.

### 제거

- `print-html`(단일 파일 base64 인라인) 기능을 제거했다.

## 2026-05-26

- sender / receiver GUI 워크플로를 추가했다(명령·옵션 없이 실행 시 GUI 진입).

## 2026-04-24

- slide 기본값을 properties로 설정할 수 있게 했다.
- Gradle 빌드 설정과 상위 문서 구성을 정리했다.

## 2026-03-29

- 실제 실행 가능한 애플리케이션 버전을 개발했다.

## 2026-02-20

- air-bridge 초기 구현: encode / decode / play CLI, jar·zip 전송 전후 blacklist
  확장자 우회 helper, OS별 jar 분리 빌드, 빌드 버전 표기.
